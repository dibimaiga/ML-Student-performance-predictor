## End to end Machine Learning project on Student performance dataset
A small end-to-end machine learning project to predict student performance (Math score) from student-related features, with a simple app/API for inference.

- Data loading + basic preprocessing
- Model training + evaluation
- Saved artifacts (trained model + preprocessing if applicable)
- A simple interface to run predictions (web app via Flask)
- Deployed on Azure
